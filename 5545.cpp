#include <iostream>

using namespace std;

typedef long long ll;

int n, a, b;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> a >> b >> n;

    ll af = n / (a + 1);
    ll bf = n / (b + 1);

    
}