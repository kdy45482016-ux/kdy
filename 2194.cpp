#include <iostream>
#include <vector>

using namespace std;

int n, s;
vector <int> c;
vector <int> y;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    for(int i = 0; i < n; i++){
        cin >> c[i] >> y[i];
    }
}